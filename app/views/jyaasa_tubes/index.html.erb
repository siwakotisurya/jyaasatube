<div class="container-fluid">
	<div class="col-md-10 col-md-push-2" id="content_top">
		<h4>Videos</h4>
	<%@videos.each do |s|%>
			<%= video_tag s.video, width: "280", height: "200", controls: true %>
			<%end%>

	</div>

	<div class="col-md-10 col-md-push-2" id="content_top">
			<h4>Channels</h4>
			<%@channels.each do |c|%>
				<div class="col-md-2">
				Channel: <%=c.name%><br><br>
				<%=link_to "subscribe",  new_channel_subscription_path(c.name)%>
				</div>
			<%end%>
	</div>
</div>
